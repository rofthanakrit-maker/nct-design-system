# Slide Design System — NCT · v4

ส่วนต่อขยายจาก v1 / [v2](slide-design-system-v2.md) / [v3](slide-design-system-v3.md)
วางด้วยวิธีของ skill `dataviz`: เลือกรูปแบบก่อน → กำหนดสีตามหน้าที่ → **รัน validator
ไม่ใช้ตาดู** → mark spec → accessibility

**สิ่งที่ v1–v3 ทำถูกแล้วไม่แตะ** — canvas, grid, จังหวะแนวตั้ง, type scale, brand mode,
status token, กติกาการทำเด็คทั้งหมด

**สิ่งที่ v4 เพิ่ม** — ชุดสีข้อมูลที่ผ่านการตรวจ, กติกา mark ของกราฟ และ layout 19–23
เพราะ v1 §7 เขียนไว้ว่า "ยังไม่มี chart style" และ deck ข้อเสนอจริงต้องมีกราฟ แผนงาน และหน้าราคา
และ PowerPoint หยิบ theme accent1–6 มาใช้เอง ซึ่งเดิมเป็นน้ำเงิน 4 เฉดกับ `PAPER2`
(series ที่ 6 เลยมองไม่เห็นบนพื้นขาว)

> ลำดับการแก้ยังเหมือนเดิม: `design.md` → `scripts/tokens.py` → `python scripts/build.py`
> **สถานะ (2026-09-11)** — §2 ลง `design.md § v4` และ `tokens.py` แล้ว · §2.2 เจ้าของระบบอนุมัติแล้ว ·
> L19 build แล้วทั้ง `.potx` และ React · **L20–L23 ยังเป็น spec** · web ยัง emit แค่ `--nct-cat-*`,
> `--nct-seq-*` / `--nct-div-warm-l` จะ emit วันที่มี layout อ่านมัน

---

## 1. ทำไมสีหมวดเดิมใช้กับกราฟไม่ได้

รัน validator กับ `CAT_1..4` (navy · teal · teal-l · deep) บน `PAPER`:

```
node <dataviz>/scripts/validate_palette.js "#23436D,#216B7F,#4E8FA8,#16324F" --surface "#FFFFFF"
  [FAIL] Lightness band       #23436D L 0.38 · #16324F L 0.31  (ต้อง 0.43–0.77)
  [FAIL] Chroma floor         ทั้ง 4 ตัว C 0.063–0.082         (ต้อง ≥ 0.10 ไม่งั้นอ่านเป็นเทา)
  [PASS] CVD separation       worst 11.8
  [FAIL] Normal-vision floor  #216B7F↔#23436D ΔE 12.2          (ต้อง ≥ 15)
  [PASS] Contrast vs surface
```

v2 §3.3 เจอปัญหาเดียวกันมาก่อนแล้ว คือสี่สีห่างกันแค่ 1.31:1 ต้องพึ่ง `CategoryKey` บนทุกสไลด์
ในตารางยังพอไหวเพราะมีตัวอักษรกำกับทุกแถว **แต่ในกราฟ สีต้องแยก series ได้เอง** เส้นกราฟสีน้ำเงินสี่เส้นที่ตัดกันไปมา
key ก็ช่วยให้อ่านออกไม่ได้

---

## 2. Color tokens สำหรับข้อมูล

ใช้ร่วมกันทั้งสอง brand เหมือน status และ category (v3 §2) กราฟต้องสื่อความหมายเดียวกันทุก brand
**กราฟอยู่บน `PAPER` เท่านั้น** ห้ามวางบนสไลด์เข้ม (01 · 02 · 08 · 10 · 15) ระบบเลยไม่มี dark variant

### 2.1 Categorical — identity (series / หมวด)

| Slot | Token | HEX | hue | บน `PAPER` | บน `PAPER2` | ที่มา |
|---|---|---|---|---|---|---|
| 1 | `CAT_1` | `#2A5EA0` | น้ำเงิน 256° | 6.55:1 | ≥ 3:1 | hue เดียวกับ `NAVY` ยก L/C เข้า band |
| 2 | `CAT_2` | `#BB731B` | อำพัน 62° | 3.76:1 | ≥ 3:1 | ขั้วอุ่นตัวเดียวของระบบ |
| 3 | `CAT_3` | `#0D9298` | teal 200° | 3.76:1 | ≥ 3:1 | `TEAL_B` (ปลายโลโก้) ยก chroma — ΔE 1.5 จากต้นฉบับ |
| 4 | `CAT_4` | `#6C4289` | ม่วง 320° | 7.56:1 | ≥ 3:1 | |
| — | `CAT_MUTE` | `#A4A4A4` | เทา | 2.49:1 | — | "อื่น ๆ" · series ที่ถูกลดบทบาท · เส้นฐาน |

ผล validator (ลำดับ slot นี้เท่านั้น — **ลำดับคือกลไกกันตาบอดสี ห้ามสลับ**):

```
adjacent · PAPER   CVD worst 15.0 (CAT_4↔CAT_3 deutan) · normal worst 21.4 · contrast ≥ 3:1 · ALL PASS
adjacent · PAPER2  ALL PASS (ค่าเดียวกัน)
all-pairs · slot 1–3   CVD 15.2 · normal 15.9 · ALL PASS
all-pairs · slot 1–4   FAIL — CAT_4↔CAT_1 CVD 4.0 (deutan)
```

แปลว่า
- **กราฟที่สีแค่อยู่ติดกัน** (stacked, bar กลุ่ม, เส้น) รับได้ 4 series
- **กราฟที่สีไหนก็อาจไปอยู่ข้างกันได้** (scatter, small multiples) รับได้ **3** เพราะคนตาบอดสีแดง-เขียวแยกม่วงกับน้ำเงินไม่ออก
- series ที่ 5 ขึ้นไป **ห้ามสร้างสีใหม่** ให้รวมเป็น "อื่น ๆ" (`CAT_MUTE`) หรือแยกสไลด์

เหตุผลของหลายค่าในตาราง
- `CAT_3` ไม่ใช้ `TEAL_B` ตรง ๆ เพราะ `TEAL_B` มี C 0.094 ต่ำกว่าเกณฑ์ และ all-pairs กับ `CAT_1`
  ได้ normal ΔE 14.8 (ตก) ส่วน `#0D9298` ตาคนมองแทบไม่ต่างจากโลโก้ และผ่านเกณฑ์ทุกข้อ `TEAL_B` ยังเป็น
  gradient stop อย่างเดียวเหมือนเดิม
- `CAT_MUTE` เป็นเทาอ่อนสุดที่ยังห่างทุก slot ≥ 15 (ใกล้สุด `CAT_3` 15.5) เทาเข้มกว่านี้ไปชน `CAT_3`
  (`#8F8F8F` ได้ 11.2) ค่านี้บังเอิญเป็น `INK2` ตัวเก่าที่ถูกถอดเพราะ contrast ตัวอักษรไม่ผ่าน
  **ใช้เป็น mark ได้ ห้ามใช้เป็นตัวอักษร** และเพราะต่ำกว่า 3:1 ทุกที่ที่ใช้ต้องมีตัวเลขหรือ legend กำกับเสมอ
- ห่างจาก status ทุกคู่ ≥ 15.7 (ใกล้สุด `CAT_1`↔`OK`) — ไม่มี series ไหนดูเหมือนสถานะ

### 2.2 ใช้ชุดนี้แทนค่า `CAT_1..4` เดิม — อนุมัติแล้ว

**เปลี่ยนค่า `CAT_1..4` เป็นชุดใหม่ ไม่เพิ่ม `DATA_*` เป็นชุดที่สอง** เพราะ
กฎของ dataviz คือสีต้องติดกับตัวข้อมูลไปทั้งเด็ค ถ้าตาราง L16 ให้ AP เป็นน้ำเงินแต่กราฟให้ AP เป็นอำพัน
ผู้อ่านจะเข้าใจผิดทันที ถ้ามีสองชุดก็ต้องห้ามวางตารางกับกราฟหมวดเดียวกันไว้ในเด็คเดียว ซึ่งทำจริงไม่ได้

สิ่งที่เปลี่ยนตาม (ตรวจแล้ว contrast ≥ 3:1 บน `PAPER` และ `PAPER2` ทุกจุด)
- L12 tab และเลขการ์ด · L14 ขอบกับ tab ของกล่องส่วนที่เพิ่ม · L16 เส้นใต้คอลัมน์แรก · `CategoryKey`
- สี่หมวดจะแยกกันด้วยสีได้จริง `CategoryKey` ยังต้องมี (identity ต้องไม่ฝากไว้ที่สีอย่างเดียว)
  แต่ไม่ต้องใช้จำว่าน้ำเงินไหนเป็นหมวดไหนอีกแล้ว
- `check_template.py` ยกเว้น shape `CAT_CODED` ไว้ เพราะ `CAT_2` เป็น `TEAL` ซึ่งเป็นเทาบ้านที่ห้ามโผล่ในไฟล์ corp
  พอเปลี่ยนค่าแล้วข้อยกเว้นนี้**ลบได้**
- **hardcode ที่ต้องแก้พร้อมกัน** — `build.py:462` `cats16 = [NAVY, TEAL, TEAL_L, DEEP]` และ
  `parts_layouts.py:414` `CATS = [...]` สองจุดนี้ไม่ได้อ่านค่าจาก `CAT_*` แก้แค่ `tokens.py` จะไม่มีผลกับสองจุดนี้

demo ของ L16 ที่เคยข้ามจากหมวด 2 ไปหมวด 4 (GL) ย้ายมาใช้หมวด 3 แล้ว — slot ใช้ตามลำดับ ห้ามข้าม

### 2.3 Sequential — ขนาด (มาก = เข้ม) · hue เดียว 256°

| step | token | HEX | บน `PAPER` |
|---|---|---|---|
| 100 | `SEQ_100` | `#DBE9FC` | 1.23 |
| 200 | `SEQ_200` | `#B7D0F2` | 1.58 |
| 300 | `SEQ_300` | `#90B4E4` | 2.14 |
| 400 | `SEQ_400` | `#6994CF` | 3.12 |
| 500 | = `CAT_1` | `#2A5EA0` | 6.55 |
| 600 | = `NAVY` | `#23436D` | 10.02 |
| 700 | = `DEEP` | `#16324F` | 13.10 |

เพิ่มแค่ 4 hex ส่วน 3 ขั้นบนใช้ของเดิม ramp ต่อกับสีแบรนด์พอดี
`--ordinal` ทั้ง 7 ขั้น: L ไล่ถูกทิศ · ΔL ≥ 0.06 ทุกขั้น · hue ห่างกันแค่ 5°
- **continuous** (heatmap) ใช้ได้ทั้ง 100–700 ขั้นอ่อนจางไปทางพื้นได้ ต้องมีแถบ scale กำกับ
- **ordinal** (ขั้น funnel, tier, S/M/L) เริ่มที่ **300** ขึ้นไป ขั้นอ่อนสุดต้องได้ ≥ 2:1 (`SEQ_300` 2.14 ผ่าน)
- ห้ามใช้ ramp นี้แทนสีหมวด (ห้ามให้สินค้าที่ขายดีกว่าได้สีเข้มกว่า — แท่งยาวบอกอยู่แล้ว)

### 2.4 Diverging — อยู่ฝั่งไหนของเส้นฐาน

| ขั้ว | เข้ม | อ่อน |
|---|---|---|
| เย็น (ต่ำกว่า / ลด) | `CAT_1` `#2A5EA0` | `SEQ_300` `#90B4E4` · 2.14 |
| กลาง | `RULE` `#E5E5E5` | |
| อุ่น (เกิน / เพิ่ม) | `CAT_2` `#BB731B` | **`DIV_WARM_L`** `#D6A67C` · 2.19 |

แขนละ 2 ขั้น ความสว่างของขั้นอ่อนสองฝั่งพอ ๆ กัน (2.14 / 2.19) ห้ามมีสีตรงกลาง ขั้วต้องอุ่น/เย็นตรงข้ามกัน
ใช้เมื่อสีบอกแค่ **ทิศ** ถ้าทิศนั้นแปลว่าดีหรือแย่ชัดเจน (SLA ผ่าน/ไม่ผ่าน) ให้ใช้ `OK` / `RISK`
พร้อมไอคอนแทน — **กราฟหนึ่งรูปห้ามมีทั้งสองแบบ**

### 2.5 Theme slot ของ PowerPoint

กราฟที่กด Insert Chart ใน `.potx` หยิบสีจาก accent ตามลำดับ ต้องชี้ใหม่
ไม่งั้น validator ข้างบนก็ไม่มีความหมาย

| slot | เดิม | ใหม่ |
|---|---|---|
| accent1–4 | `NAVY` `TEAL` `TEAL_L` `DEEP` | `CAT_1` `CAT_2` `CAT_3` `CAT_4` |
| accent5 | `INK2` | `CAT_MUTE` (series ที่ 5 = "อื่น ๆ") |
| accent6 | `PAPER2` (มองไม่เห็นบนขาว) | `TEAL` (ให้ยังหยิบ teal จาก theme picker ได้) |

ตรวจแล้ว `scripts/` ไม่มีที่ไหนอ้าง `schemeClr accent*` เลย การเปลี่ยนนี้กระทบแค่กราฟกับ
SmartArt ที่ผู้ใช้ insert เอง (SmartArt จะเปลี่ยนจาก navy เป็น `CAT_1`)

---

## 3. Mark spec — ใช้กับทุกกราฟใน L19–L23

หน่วยฝั่งเว็บเป็น px บน canvas 1280×720 (96 dpi) · 1 px = 0.75 pt

| Mark | spec |
|---|---|
| แท่ง (bar/column) | หนา **≤ 0.250in (24px)** ไม่ต้องเต็มช่อง · เว็บ: ปลายมน 4px ฐานเหลี่ยม · PPT: เหลี่ยม (native chart ทำปลายมนข้างเดียวไม่ได้ — ยอมรับ) |
| เส้น | **1.5 pt (2px)** round join/cap |
| จุด / จุดปลายเส้น | **≥ 10px (0.104in)** สีของ series + **วงแหวน `PAPER` 2px** |
| พื้นใต้เส้น | series เดียวเท่านั้น · สีของ series ที่ 10% |
| ช่องไฟระหว่างชิ้นที่ติดกัน | **`PAPER` 2px (1.5 pt)** ใช้ช่องไฟแยก ห้ามตีเส้นขอบรอบแท่ง |
| gridline | `RULE` 0.75 pt ทึบ · column/line = แนวนอน · bar/แผนงาน = แนวตั้ง · **ห้ามเส้นประ** |
| เส้นฐาน / เส้นเป้า | `CAT_MUTE` 0.75 pt ทึบ · เส้นเป้ามีป้ายตรงปลาย ("เป้า 95%") |

**Type roles ที่เพิ่ม — ใช้ได้เฉพาะ L19–L23**

| Role | Font | Size | สี | หมายเหตุ |
|---|---|---|---|---|
| Tile value | Kanit Bold | 40 pt | สีตัวเลขเด่นตาม L06 ของ brand | L20 · L23 · ตัวเลขกว้างตามปกติ (ไม่ใช้ tabular) |
| Axis tick | Noto Sans Thai | 12 pt | `INK2` | `tabular-nums` · มีคอมมาหลักพัน · หน่วยใส่ในชื่อแกน ไม่ใส่ทุก tick |
| Direct label | Noto Sans Thai Bold | 14 pt | `INK` | ค่าที่ปลายแท่ง / ปลายเส้น |
| Legend / row label | Noto Sans Thai | 12 / 14 pt | `INK` | |
| Milestone label | Noto Sans Thai | 10 pt | `INK2` | L22 เท่านั้น |
| Source note | Noto Sans Thai | 10 pt | `INK2` | แถว note เดียวกับ L16 |

**กติกาป้ายและ legend**
- **ตัวอักษรไม่ใช้สีของ series** ค่า ป้าย legend ใช้ `INK` / `INK2` เท่านั้น สีบอกตัวตนผ่าน swatch หรือเส้นสั้นที่อยู่ข้าง ๆ
- **ตั้งแต่ 2 series ต้องมี legend** (มุมซ้ายบนของกราฟ แถวสูง 0.300in) · series เดียวไม่ต้องมี หัวเรื่องบอกแล้ว
- ใส่ป้ายค่า**เฉพาะจุดที่เล่าเรื่อง** — จุดปลาย จุดสูงสุด หรือ series ที่เป็นประเด็น ห้ามใส่ตัวเลขทุกจุด
- ป้ายไม่พอดีในแท่ง → ย้ายไปไว้นอกปลายแท่ง ห้ามตัดคำ
- ปลายเส้นห่างกันไม่ถึงหนึ่งบรรทัดป้าย (28.8px) → ตัดป้ายปลายเส้นทิ้งทั้งชุด ให้ legend ทำงาน ห้ามดันป้ายขึ้นลงหนีกัน
- แกนตั้งของ bar/column **เริ่มที่ 0 เสมอ** · 4–6 tick ค่ากลม ๆ

**ห้ามทั้งระบบ** — กราฟสองแกนตั้ง (แยกเป็นสองกราฟ หรือ index ทั้งคู่เทียบค่าเริ่มต้น = 100) · pie/donut
(ใช้ L23) · 3-D · เงา · gradient ในกราฟ · เปลี่ยนสีตัวที่เหลือตอนเอา series ออก · สีที่ 5

**ตัวเลขต้องมีที่อ่าน** — กราฟทุกรูปต้องมีตัวเลขครบ ถ้าไม่อยู่บนสไลด์ (L21 · L23 แบบแท่งเดียว)
ให้ใส่ตาราง L16 ในภาคผนวก แล้วเขียนที่ source note ว่า "ตัวเลขทั้งหมด: ภาคผนวก ก"
ฝั่งเว็บทุก mark มี `<title>` และในกราฟมี `<table>` ซ่อนไว้ให้ screen reader
(`ponytail:` ไม่ทำ tooltip JS เพราะเด็คใช้นำเสนอ ไม่ได้ให้คนกดดูข้อมูล ถ้าเด็คไหนเปิดเป็น dashboard ค่อยเพิ่ม)

---

## 4. Layout ที่เพิ่ม — 19 ถึง 23

ทุกอันใช้ margin, จังหวะแนวตั้ง, `showMasterSp="0"`, `<a:lstStyle>` บนทุก placeholder
และ chrome ตาม brand เหมือน v2/v3 · layout ที่มี placeholder เกิน 9 ตัวเริ่ม idx ที่ `PH_FREE = 20` (v3 §7)
ส่วนล่างของทุกอันเหมือน L16: **source note y 5.750 · takeaway band y 6.150** เนื้อหาจบที่ **5.650**

| # | Layout | งานของข้อมูล | ใช้แทน | ฝั่ง `.potx` |
|---|---|---|---|---|
| 19 | Chart + Insight | ขนาด · แนวโน้ม · สัดส่วนตามเวลา · ส่วนต่างจากเป้า | กราฟที่วางลอยใน L03 | `chart` PH — native chart |
| 20 | KPI + Trend | ตัวเลขหลัก 3–4 ตัว + แนวโน้มของหนึ่งในนั้น | L06 ที่ต้องมีส่วนต่าง | text PH + `chart` PH (sparkline เว็บเท่านั้น) |
| 21 | Before → After | เปรียบเทียบก่อน/หลังรายรายการ (ROI) | ตาราง 2 คอลัมน์ | text PH + shape (ไม่มี dumbbell แบบ native) |
| 22 | Plan Timeline | ช่วงเวลาของเฟส + จุดส่งมอบ | ตาราง Gantt ระบายสีเซลล์ | `chart` PH — stacked bar ซ่อนแท่งแรก |
| 23 | Composition | ส่วนของทั้งหมด · เทียบแพ็กเกจราคา | pie · หน้าราคาที่ยังไม่มี | text PH + `chart` PH — 100% stacked bar |

แนะนำให้ทำ **19 → 22 → 23 → 20 → 21** L19 ใช้ได้กับงานส่วนใหญ่ ส่วน 22/23 เป็นช่องว่างที่ v1 §7 จดไว้
(timeline, pricing)

### 19 Chart + Insight

พื้นขาว · กราฟ 8 คอลัมน์ + insight 4 คอลัมน์

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold ตาม brand — **เขียนเป็นข้อสรุป** |
| Accent Rule | ตาม brand | | | | |
| Chart PH (`chart`) | 1.000 | 1.850 | 7.489 | 3.800 | ดูโครงข้างใน ↓ |
| Figure PH | 8.688 | 1.850 | 3.644 | 1.100 | 72 pt Kanit Bold — ตัวเลขเดียวที่อยากให้จำ |
| Figure label PH | 8.688 | 2.950 | 3.644 | 0.500 | 12 pt Bold `spc 120` `INK2` · ≤ 2 บรรทัด |
| Insight PH | 8.688 | 3.600 | 3.644 | 2.050 | 16 pt `INK` · bullet ≤ 3 |
| Source PH | 1.000 | 5.750 | 11.333 | 0.300 | 10 pt `INK2` |
| Takeaway band + label + copy | | 6.150 | | | เหมือน L16 ทุกค่า |

**โครงใน Chart PH (7.489 × 3.800)** — legend 0.300 บน (เฉพาะ ≥ 2 series) · ช่อง tick แกนตั้ง 0.600
ซ้าย · **แถบแกนนอน 0.350 ล่างนับรวมในกรอบ** (กรอบที่ไม่นับแถบนี้คือกราฟที่ป้ายแกนล้น) · เหลือพื้นที่วาด
6.889 × 3.150 (ไม่มี legend 3.450)

| รูปแบบ | ใช้เมื่อ | สี | เพดาน |
|---|---|---|---|
| column | ขนาดตามช่วงเวลา / หมวด | series เดียว = `CAT_1` ทุกแท่ง · เน้นแท่งเดียว = `CAT_1` ที่เหลือ `CAT_MUTE` | 12 แท่ง |
| bar แนวนอน | รายการชื่อยาว (ชื่อไทย) | เหมือน column · เรียงจากมากไปน้อย | 8 แท่ง |
| line | แนวโน้ม | `CAT_1..4` ตามลำดับ · เน้นเส้นเดียว = `CAT_1` + `CAT_MUTE` | 4 เส้น |
| stacked column | สัดส่วนตามเวลา | `CAT_1..4` เรียงจากฐาน · ส่วนที่เกินรวมเป็น `CAT_MUTE` บนสุด | 4 + อื่น ๆ |
| diverging bar | ส่วนต่างจากเป้า/งบ | §2.4 | 8 แท่ง — **ยังไม่มีใน `Chart` ฝั่งเว็บ** |

- **รูปแบบที่ควรใช้ก่อนคือเน้นตัวเดียว** ถ้าเรื่องที่เล่าคือ "ตัวนี้ขึ้น" ให้ไฮไลต์ตัวนั้นแล้วทำที่เหลือเป็นเทา
  ไม่ต้องให้ทุกตัวมีสีของตัวเอง
- Figure กับกราฟต้องเล่าเรื่องเดียวกัน ถ้าตัวเลขเด่นไม่ได้มาจากกราฟ ใช้ L06
- ความหนาแท่ง = `min(0.250in, ช่อง × 0.6)` · tick ขั้นละ 1 / 2 / 5 ได้ 4–6 ขีด (PowerPoint ตั้งเองได้ 11 ขีดบนช่วง 0–9 เลยต้องใส่ `majorUnit`)
- `.potx`: กด Insert Chart บน placeholder ได้สีจาก theme ที่ชี้ใหม่ใน §2.5 · สไลด์เดโมเป็นกราฟ native ที่ใส่ค่าตรง ๆ
  (`c:numLit`) ไม่มี workbook ฝัง — แก้ style ตามได้ แต่ Edit Data เปิดไม่ได้

### 20 KPI + Trend

พื้นขาว · tile 3 ใบบน `THIRD` หรือ 4 ใบบน `QUARTER` + กราฟแนวโน้มเต็มกว้าง

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH · Accent Rule | | | | | เหมือน L19 |
| Tile rule × 3/4 | ตาม grid | 1.850 | THIRD / QUARTER | 0.014 | `RULE` · **tile ที่กราฟข้างล่างพล็อตใช้ `CAT_1` สูง 0.050** |
| Tile label PH | +0 | 1.980 | เต็ม tile | 0.300 | 12 pt Bold `spc 120` `INK2` |
| Tile value PH | +0 | 2.280 | เต็ม tile | 0.700 | Tile value 40 pt |
| Tile delta PH | +0 | 3.000 | ครึ่ง tile | 0.300 | 12 pt `INK` นำด้วย ▲/▼ สี `OK` หรือ `RISK` ตามว่าดีหรือแย่ + "จาก Q2" |
| Sparkline (เว็บ) | +ครึ่ง | 3.000 | ครึ่ง tile | 0.300 | `CAT_MUTE` 1.5 pt · จุดล่าสุด `CAT_1` 6px |
| Trend PH (`chart`) | 1.000 | 3.600 | 11.333 | 2.050 | line series เดียว `CAT_1` · เส้นเป้า `CAT_MUTE` ได้ |
| Source PH · Takeaway | | 5.750 · 6.150 | | | เหมือน L19 |

- **tile ไม่มีพื้น** ใช้เส้นบนแบ่ง sparkline `CAT_MUTE` บน `PAPER2` เหลือ 2.25:1 เลยไม่ใส่พื้นการ์ด
- ▲▼ คือ "ไอคอน + ข้อความ" ที่ status ต้องมี ขยาย v2 §3.2 ให้ **ใช้สี status กับ glyph ตัวต่างใน L20 ได้**
  (`OK` 6.9:1 · `RISK` 6.5:1 บนขาว) ตัวข้อความยังเป็น `INK`
- ส่วนต่างต้องบอกว่าเทียบกับอะไรทุกครั้ง ("จาก Q2", "เทียบเป้า")
- ถ้าไม่มีแนวโน้มให้เล่า ใช้ L06

### 21 Before → After

พื้นขาว · dumbbell 3–7 แถว ขายเรื่อง "หลังใช้ระบบดีขึ้นเท่าไร"

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH · Accent Rule | | | | | เหมือน L19 |
| Intro PH | 1.000 | 1.850 | 7.000 | 0.300 | 14 pt `INK2` — บอกหน่วยและทิศที่ดี ("วันที่ใช้ปิดงบ · น้อยกว่าดีกว่า") |
| Legend PH | 8.333 | 1.850 | 4.000 | 0.300 | 12 pt `INK` ชิดขวา · `●` ก่อน `●` หลัง |
| Scale band | 4.200 | 2.250 | 6.333 | 0.300 | tick 12 pt `INK2` **แกนอยู่บน** ตำแหน่งไม่เลื่อนตามจำนวนแถว |
| Row label PH × 7 | 1.000 | 2.600 + 0.430n | 3.000 | 0.430 | 14 pt `INK` |
| Plot zone | 4.200 | 2.600 | 6.333 | ≤ 3.010 | gridline แนวตั้ง `RULE` ตาม tick |
| Value PH × 7 | 10.733 | 2.600 + 0.430n | 1.600 | 0.430 | 14 pt Bold `INK` ชิดขวา `tabular-nums` — ส่วนต่างมีเครื่องหมาย ("−8 วัน") |
| Source PH · Takeaway | | 5.750 · 6.150 | | | |

**Mark** — จุดก่อน `SEQ_300` 10px · จุดหลัง `CAT_1` 10px · ทั้งคู่มีวงแหวน `PAPER` 2px ·
เส้นเชื่อม `SEQ_300` 1.5 pt (สีเดียวสองเฉดตาม dataviz)
`SEQ_300` ได้ 2.14:1 ถ้าอยู่คนเดียวไม่ผ่าน แต่ใช้ได้เพราะ**คอลัมน์ค่ามีทุกแถวเสมอ** ห้ามซ่อนคอลัมน์นี้

- ทุกแถวต้องใช้หน่วยเดียวกัน หน่วยปนกันให้แปลงเป็น % ของค่าเดิม หรือแยกสไลด์
- เรียงแถวจากส่วนต่างมากสุดลงมา · 7 แถวคือเพดาน (2.600 + 7×0.430 = 5.610)
- ฝั่ง `.potx` ไม่มี chart แบบนี้ให้ใช้ — layout ให้แค่ PH ข้อความ ส่วนเดโมจะวางจุดกับเส้นเป็น shape ให้ copy ไปแก้ตำแหน่งเอง
  (`ponytail:` วางตำแหน่งด้วยมือ ถ้าใช้บ่อยค่อยทำเป็น scatter chart ที่ตั้ง style ไว้แล้ว)

### 22 Plan Timeline

พื้นขาว · ภาพรวมแผน L17 คือรายละเอียดทีละเฟส **เลขเฟสต้องตรงกัน**

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH · Accent Rule | | | | | เหมือน L19 |
| Period header PH | 3.900 | 1.850 | 8.433 | 0.300 | 12 pt Bold `INK2` กลางช่อง · ≤ 12 ช่อง ("ด.1" … "ด.12") |
| Row label PH × 8 | 1.000 | 2.250 + 0.425n | 2.100 | 0.425 | 14 pt `INK` · เลขเฟส Kanit Bold นำหน้า |
| Duration PH × 8 | 3.100 | 2.250 + 0.425n | 0.600 | 0.425 | 12 pt `INK2` ชิดขวา `tabular-nums` ("8 สัปดาห์") |
| Plot (`chart`) | 3.900 | 2.250 | 8.433 | 3.400 | gridline แนวตั้ง `RULE` ที่ขอบทุกช่วงเวลา |
| Key PH | 1.000 | 5.750 | 5.667 | 0.300 | 10 pt `INK2` · `■` งาน `◆` ส่งมอบ/ตรวจรับ |
| Assumption PH | 6.667 | 5.750 | 5.667 | 0.300 | 10 pt `INK2` ชิดขวา ("นับจากวันลงนามสัญญา") |
| Takeaway | | 6.150 | | | ("Go-live เดือนที่ 7 · ตรวจรับ 3 งวด") |

**Mark** — แท่ง `CAT_1` หนา 0.200in กลางแถว (เว็บ: มนทั้งสองปลาย เพราะไม่มีฐาน) ·
เน้นเฟสเดียว = `CAT_1` ที่เหลือ `SEQ_300` (ใช้ได้เพราะป้ายเฟสมีทุกแถว) ·
จุดส่งมอบ `◆` 0.160in `CAT_2` + วงแหวน `PAPER` · ชื่อจุดส่งมอบ 10 pt ไว้ขวามือถ้าวางได้ ไม่งั้นย้ายไปไว้ในตารางแผน

- **ใช้ช่วงเวลาแบบนับจากวันเริ่ม** ("เดือนที่ 1") ตอนทำข้อเสนอยังไม่รู้วันเริ่มจริง
- เกิน 8 แถวให้รวมเป็นเฟสแล้วลงรายละเอียดใน L17 · เกิน 12 ช่วงให้เปลี่ยนหน่วย (สัปดาห์ → เดือน → ไตรมาส)
- ไม่ใส่ข้อความในแท่ง ไม่ใส่ลูกศร dependency (อยากโชว์ลำดับการทำงานใช้ L13 / L14)
- `.potx`: stacked bar ที่ series แรก (จุดเริ่ม) เป็น `noFill` ส่วน series ที่สอง (ระยะเวลา) เป็น `CAT_1` ·
  ตั้ง gap width ให้แท่งหนา 0.200in · แก้ตัวเลขใน data sheet ได้ · จุดส่งมอบวางเป็น shape เอง

### 23 Composition

พื้นขาว · ส่วนของทั้งหมด 1 แท่ง (งบแยกหมวด) หรือ 2–3 แท่ง (เทียบแพ็กเกจ) — **แทน pie ทุกกรณี**

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH · Accent Rule | | | | | เหมือน L19 |
| Total PH | 1.000 | 1.850 | 5.565 | 0.800 | Tile value 40 pt ("4.8 ล้านบาท") |
| Total label PH | 1.000 | 2.650 | 5.565 | 0.300 | 12 pt Bold `spc 120` `INK2` |
| Intro PH | 6.768 | 1.850 | 5.565 | 1.100 | 14 pt `INK2` — เงื่อนไข (รวม VAT, ระยะสัญญา) |
| Bar label PH × 3 | 1.000 | 3.200 + 0.500n | 1.800 | 0.500 | 14 pt Bold `INK` (ชื่อแพ็กเกจ) |
| Bars (`chart`) | 2.950 | 3.200 | 9.383 | ≤ 1.500 | แท่งหนา 0.250in · ช่อง `PAPER` 2px ระหว่างส่วน |
| Breakdown × 4 | QUARTER | 4.850 | 2.683 | 0.800 | swatch 0.139in + ชื่อ 12 pt `INK` · ค่า 16 pt Bold `INK` `tabular-nums` · % 12 pt `INK2` |
| Source PH · Takeaway | | 5.750 · 6.150 | | | |

- **ไม่เกิน 4 ส่วนรวม "อื่น ๆ"** ถ้ามีมากกว่า 4 ให้เอา 3 ส่วนใหญ่สุด แล้วรวมที่เหลือเป็น `CAT_MUTE` ในช่องที่ 4
- ลำดับส่วน = ลำดับ slot เหมือนกันทุกแท่ง **สีติดกับหมวด ไม่ติดกับขนาด** ต่อให้หมวดเล็กกว่าก็ไม่ย้ายช่อง
- ไม่ใส่ป้ายในแท่ง ตัวเลขอยู่ที่ breakdown แถวเดียว
- 1 แท่ง: breakdown แสดงค่าจริง · 2–3 แท่ง: breakdown เป็นแค่ legend ส่วนยอดรวมใส่ที่ปลายแท่ง
  (โหมด `amount` แท่งยาวตามยอด) และตัวเลขเต็มอยู่ภาคผนวก
- แพ็กเกจที่แนะนำ: ป้ายชื่อเป็น `NAVY`/accent และมีแถบ accent 0.050in เหนือป้าย — ใช้วิธีเดียวกับคอลัมน์แนะนำของ L09
- ยอดปัดเศษแล้วรวมไม่ถึง 100 ให้เขียนที่ source note ว่า "ปัดเศษ"

---

## 5. ฝั่ง React (`@nct/slides`)

วาดเป็น SVG inline **ไม่เพิ่ม chart library** เพราะรูปแบบกราฟมีแค่ห้าแบบ เขียน scale ค่ากลม ๆ เองได้ในไม่กี่บรรทัด
ทุก component อ่าน `--nct-cat-*` / `--nct-seq-*` ห้ามเขียน hex ตรง ๆ · เพดานจำนวนเป็น tuple union
ให้ compiler ตรวจแทนคน (แบบเดียวกับ `items` ของ L15)

```tsx
// ทำแล้ว - chart เป็น prop ก้อนเดียว ไม่ปนกับ prop ของ chrome
<SlideChart title="ปิดงบ พ.ค. ใช้ 9 วัน นานสุดในรอบครึ่งปี"
  chart={{ kind: 'column', unit: 'วันทำการ', categories: ['ม.ค.', …], highlight: 4,
           series: { name: 'เวลาปิดงบ', values: [6, 7, 6, 8, 9, 7] } }}
  figure={{ value: '9 วัน', label: '…' }} insights={['…', '…']} source="…" takeaway="…" />

// ยังเป็นสเปก
<SlideKpiTrend tiles={[t1, t2, t3]} trend={{ tile: 0, points: […], target: 95 }} … />
<SlideBeforeAfter better="lower" unit="วัน" rows={[{ label, before, after }, …]} … />   // 3–7
<SlidePlan periods={['ด.1', …]} rows={[{ label, start, end, milestone? }, …]} … />        // ≤8 × ≤12
<SlideComposition total="4.8 ล้านบาท" segments={['FN', 'AP', 'AR', 'อื่น ๆ']}
  bars={[{ label: 'Standard', values: […], recommended: true }]} mode="share" … />       // bars 1–3
```

- `series` / `segments` ส่งเป็นลำดับ slot ตรง ๆ **index ของสีผูกกับตัวข้อมูล ไม่ใช่ลำดับที่ render**
  เอา series ไหนออก สีของตัวที่เหลือต้องไม่เปลี่ยน
- `demo.tsx` เพิ่มสไลด์ละ 1 ใบ แล้วอัปเดต `.design-sync/conventions.md` ว่ากราฟต้องใช้ L19–L23

---

## 6. ลำดับการลงมือ

สถานะ 2026-09-11: ข้อ 1–5 ทำแล้ว · ข้อ 6–8 ทำแล้วเฉพาะ L19

1. **ตัดสินใจ §2.2** แล้วลง `design.md § v4`
2. `tokens.py` — เปลี่ยนค่า `CAT_1..4` · เพิ่ม `CAT_MUTE` `SEQ_100..400` `DIV_WARM_L`
3. แก้ hardcode `build.py:462` กับ `parts_layouts.py:414` ให้อ่าน `CAT_*`
4. `parts_theme.py` accent1–6 ตาม §2.5
5. `check_template.py` — ลบข้อยกเว้น `CAT_CODED` ของ corp แล้วรันให้ผ่านทั้ง 4 ไฟล์
6. `parts_layouts.py` L19 → 22 → 23 → 20 → 21 · ขยาย `LAYOUTS`
7. `emit_web_tokens.py` → component ใน `layouts.tsx` → `demo.tsx`
8. `render_previews.py` บนเครื่องที่มี PowerPoint แล้ว**ดูภาพจริง** validator ตรวจสีได้ แต่ป้ายชนกันหรือล้นกรอบต้องดูด้วยตา

token เปลี่ยนเมื่อไร ให้รัน validator ซ้ำ:

```bash
node <dataviz>/scripts/validate_palette.js "#2A5EA0,#BB731B,#0D9298,#6C4289" --surface "#FFFFFF"
node <dataviz>/scripts/validate_palette.js "#2A5EA0,#BB731B,#0D9298" --surface "#FFFFFF" --pairs all
node <dataviz>/scripts/validate_palette.js "#90B4E4,#6994CF,#2A5EA0,#23436D,#16324F" --ordinal
```

---

## 7. สรุปการเปลี่ยนแปลง

| | v3 | v4 |
|---|---|---|
| layout | 18 | **19** (+ สเปก 20–23) |
| colour token | 26 | **31** (+ `CAT_MUTE` · `SEQ_100..400` · `DIV_WARM_L` − `TEAL_L` ที่ไม่มีใครอ่านแล้ว) · `CAT_1..4` เปลี่ยนค่า |
| สีหมวด | น้ำเงิน 4 เฉด ห่างกัน 1.31:1 | 4 hue · ผ่าน validator (CVD 15.0 · normal 21.4) |
| theme accent | 4 น้ำเงิน + เทา + `PAPER2` | ชุด categorical ที่ตรวจแล้ว |
| type role | 20 | + 6 (L19–L23 เท่านั้น) |

**ยังไม่มีใน v4** — heatmap / risk matrix (มี sequential ramp ให้แล้ว เหลือ layout) ·
small multiples · scatter · texture สำหรับพิมพ์ขาวดำ
