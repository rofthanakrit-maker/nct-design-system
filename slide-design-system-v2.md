# Slide Design System — NCT · v2

ส่วนต่อขยายจาก [`slide-design-system.md`](slide-design-system.md) (v1) หลังเอาไปวัดกับงานจริง
`NCT Example.pptx` (26 สไลด์ · requirement proposal งานบัญชี AGE)

**สิ่งที่ v1 ทำถูกแล้วไม่แตะ** — canvas, grid 12 คอลัมน์, จังหวะแนวตั้ง, theme slot, กติกาสี,
กติกาการทำเด็ค §6 ทั้งหมด ยังใช้เหมือนเดิมทุกข้อ

**สิ่งที่ v2 เพิ่ม** — สาม token กลุ่มใหม่, type role โหมด dense, และ layout 11–16
เพราะ v1 ออกแบบมาสำหรับ *narrative deck* แต่งานจริงของ NCT ส่วนใหญ่เป็น *requirement /
proposal deck* ที่มีตาราง ผังกระบวนการ และรายการหลายชั้น ซึ่ง 10 layout เดิมรับไม่ไหว

> ลำดับการแก้ยังเหมือนเดิม: `design.md` → `scripts/tokens.py` → `py build.py`
> token ใหม่ในไฟล์นี้ต้องไปเพิ่มใน `design.md` ก่อน ไม่ใช่เพิ่มตรงนี้แล้วจบ

---

## 1. สิ่งที่ต้องแก้ใน v1 ก่อน (ห้าจุด) — **แก้ครบแล้วทั้งห้าข้อ**

ตรวจจากไฟล์ `.pptx` จริงเทียบกับเอกสาร v1 — ทั้งห้าข้อไม่ใช่บั๊ก แต่ทำให้ระบบดูแลต่อยาก

| # | จุด | สภาพจริงในไฟล์ | ควรแก้เป็น |
|---|---|---|---|
| 1 | §5 ตาราง layout 01 / 10 เขียนว่ามี placeholder `logo` | โลโก้เป็น `<p:pic>` ตายตัวใน layout แก้ในสไลด์ไม่ได้ | แก้คำในเอกสารเป็น "โลโก้ฝังในlayout" — พฤติกรรมถูกแล้ว อย่าเปลี่ยนเป็น placeholder |
| 2 | `RULE #E5E5E5` | เขียนเป็น `srgbClr` ตรง ๆ กระจายใน 10 layout | ยังไม่มี theme slot ว่าง — ให้ประกาศเป็นค่าคงที่ตัวเดียวใน `tokens.py` และห้าม hardcode ใน `parts_layouts.py` |
| 3 | gradient ของ 01 / 10 | มี stop กลาง `#1E5473` ที่ไม่อยู่ในตาราง §3 | ประกาศเป็น token `MID` (ดูข้อ 3.1) |
| 4 | เดโม slide 9 | มีแต่ title กับ intro ยังไม่เคย insert ตารางจริง | ใส่ตาราง 5 แถวลงเดโม จะได้เห็น table style ที่ theme ให้ |
| 5 | `Quote Mark` ใน L07 | 120 pt ไม่อยู่ในตาราง type §4 | เพิ่มบรรทัดแล้ว — สีจริงในไฟล์คือ `TEAL` alpha 25% ไม่ใช่ `TEAL_L` (เอกสารฉบับร่างเขียนคลาดเคลื่อน) |

---

## 2. Grid ที่ v1 มีอยู่แล้วแต่ยังไม่ได้เขียนลงเอกสาร

v1 ระบุ `HALF` และ `THIRD` ไว้ แต่ layout ใหม่ต้องใช้อีกสองแบบ ทั้งคู่คำนวณจาก
`COL 0.761in` + `GUT 0.20in` เดิม ไม่ใช่ค่าใหม่

| ชื่อ | คอลัมน์ | กว้าง (in) | ตำแหน่ง x (in) |
|---|---|---|---|
| `HALF` | 6 | 5.565 | 1.000 · 6.768 |
| `THIRD` | 4 | 3.644 | 1.000 · 4.844 · 8.688 |
| **`QUARTER`** | 3 | **2.683** | 1.000 · 3.883 · 6.766 · 9.649 |
| **`FIFTH`** | — | **2.107** | 1.000 · 3.307 · 5.613 · 7.920 · 10.227 |

`FIFTH` แบ่ง 5 ช่องเท่ากันไม่ลงตัวกับ 12 คอลัมน์ จึงใช้สูตร `(CW − 4×GUT) / 5` แทน
ใช้ได้เฉพาะ layout 13 Process Flow — ห้ามใช้กับ layout อื่น

---

## 3. Color tokens ที่เพิ่ม

### 3.1 Brand gradient (สุ่มสีจากไฟล์โลโก้จริง)

สแกน `nct-mark-color.png` แล้วพบว่า gradient ในโลโก้วิ่ง `#2A416D → #1A8D92`
แปลว่า **`TEAL #216B7F` ที่ v1 ใช้เป็นปลาย gradient จริง ๆ คือจุดกลางของโลโก้** — gradient
บน layout 01 / 10 ตอนนี้จึงจืดกว่าโลโก้ที่วางทับอยู่

| Token | HEX | ใช้กับ | ที่มา |
|---|---|---|---|
| `MID` | `#1E5473` | stop กลางของ gradient 01 / 10 | มีอยู่ในไฟล์แล้ว แค่ยังไม่ได้ประกาศ |
| `TEAL_B` | `#1A8D92` | **stop ปลายของ gradient เท่านั้น** | สุ่มจากโลโก้ (สีขวาสุดของ mark) |

**กติกา** — `TEAL_B` ใช้ได้เฉพาะเป็น gradient stop ห้ามใช้เป็นสีพื้น สีเส้น หรือสีตัวอักษร
เพราะ contrast บนพื้นขาวไม่ผ่าน AA สำหรับ body text (3.4:1) `TEAL` ยังเป็น accent เส้น/bullet
ตัวเดียวเหมือนเดิม

gradient ที่ถูกต้อง: `NAVY → MID → TEAL_B` 45° (เปิด) / กลับทิศ (ปิด)

### 3.2 Status — สำหรับตารางและผังที่ต้องบอกสถานะ

v1 เขียนไว้ว่า "ถ้าจำเป็นต้องมีสีเตือนให้เพิ่มลง `design.md` ก่อน" — งานจริงจำเป็นแล้ว
ตาราง requirement มีคอลัมน์ความเสี่ยง ระดับความยาก และสถานะ ซึ่งไม่มีสีรองรับ
(ของเดิมเลยไปหยิบ `#FF0000` ของ Office มาใช้ 58 ครั้ง)

| Token | HEX | Tint | ใช้กับ |
|---|---|---|---|
| `RISK` | `#B3261E` | `RISK_T` `#FBEAE8` | ความเสี่ยงสูง, blocker, ข้อควรระวัง |
| `WARN` | `#B26B00` | `WARN_T` `#FBF1E3` | รอยืนยัน, TBC, ต้องตัดสินใจ |
| `OK` | `#1F7A54` | `OK_T` `#E6F4EE` | พร้อมดำเนินการ, quick win, ผ่านแล้ว |

**กติกา**
- ทั้งสามตัวเป็น **สีข้อมูล ไม่ใช่สีตกแต่ง** ใช้ได้เฉพาะใน chip, cell background และ
  ตัวอักษรในตาราง — ห้ามใช้ทำหัวเรื่อง เส้น หรือพื้นสไลด์
- ใช้ตัวเข้มกับตัวอักษร ใช้ tint กับพื้น cell เท่านั้น ห้ามสลับ
- หนึ่งสไลด์ใช้ได้ไม่เกิน 3 สถานะ ถ้ามีมากกว่านั้นแปลว่าตารางควรแยกสไลด์
- ทั้งสามตัวปรับ hue มาให้เข้ากับ navy/teal แล้ว อย่าแทนด้วยแดง/เหลืองสด

### 3.3 Category coding — สีประจำหมวดงาน

งานจริงแบ่งกระบวนการเป็น 4 หมวด (FN / AP / AR / GL) แล้วให้สีแต่ละหมวด ซึ่งเป็นวิธีที่ดี
แต่ไปหยิบสีนอกระบบมาใช้ v2 ผูกมันลง accent ที่มีอยู่แล้วแทน ไม่ต้องเพิ่มสีใหม่สักตัว

| หมวด | Token | HEX | ใช้เป็น |
|---|---|---|---|
| หมวดที่ 1 | `NAVY` | `#23436D` | chip ตัวเลข, จุดนำหน้า, หัวคอลัมน์ย่อย |
| หมวดที่ 2 | `TEAL` | `#216B7F` | เหมือนกัน |
| หมวดที่ 3 | `TEAL_L` | `#4E8FA8` | เหมือนกัน |
| หมวดที่ 4 | `DEEP` | `#16324F` | เหมือนกัน |

- ผูกสีกับหมวดครั้งเดียวต่อเด็ค แล้วใช้ให้ตรงกันทุกสไลด์ — ถ้า AP เป็น TEAL ในสไลด์ตาราง
  ก็ต้องเป็น TEAL ในผังกระบวนการด้วย
- เกิน 4 หมวดให้เลิกใช้สีแยกหมวด แล้วใช้ตัวอักษรกำกับแทน — ระบบไม่มี accent ที่ 5
  ที่แยกจากกันได้ด้วยตาเปล่า

---

## 4. Typography ที่เพิ่ม — โหมด dense

งานจริงลงไปถึง **6.75 pt** ขณะที่ระดับเล็กสุดของ v1 คือ 10 pt เพราะไม่มี role รองรับ
ตารางและผัง คนทำเด็คเลยย่อเอง v2 เพิ่ม role ให้ถูกกฎ พร้อมล็อกพื้นล่างไว้ที่ 10 pt

| Role | Font | Size | Weight | Color | ใช้ได้เฉพาะ |
|---|---|---|---|---|---|
| Step number | Kanit | 24 pt | Bold | `PAPER` บน chip | L13 |
| Card number | Kanit | 24 pt | Bold | สีหมวด | L12 |
| Dense heading | Kanit | 16 pt | Bold | `NAVY` | L11 – L14 |
| Dense body | Noto Sans Thai | 12 pt | Regular | `INK` | L11 – L14 |
| Table header | Noto Sans Thai | 11 pt | Bold, `spc 60` | `PAPER` | L16 |
| Dense cell | Noto Sans Thai | 10 pt | Regular | `INK` | L16 |
| Quote mark | Kanit | 120 pt | Bold | `TEAL_L` | L07 (เอกสารเดิมตกไป) |

**กติกาโหมด dense**
- **10 pt คือพื้นล่างเด็ดขาด** ต่ำกว่านี้ไม่มี role รองรับ ถ้าเนื้อหาไม่พอต้องแยกสไลด์
  ไม่ใช่ย่อตัวอักษร
- ใช้ได้เฉพาะ L11–L14 และ L16 เท่านั้น — **L03 / L04 / L05 ยังคง 18/16/14 pt เหมือนเดิม**
  ห้ามเอา dense ไปใช้กับสไลด์ bullet ปกติ ไม่งั้นทั้งเด็คจะไหลลงไปหมด
- Kanit ยังเป็นเสียงหัวเรื่อง Noto Sans Thai ยังเป็นเสียงเนื้อหา กฎ v1 ไม่เปลี่ยน
  งานจริงใช้ Kanit ทั้งเด็ค ซึ่งคืออาการ flat voice ที่ระบบตั้งใจแก้พอดี
- ไม่ใช้ตัวเอียง กฎเดิม

---

## 5. Layout ที่เพิ่ม — 11 ถึง 16

ทั้งหกอันใช้ margin 1 นิ้ว, จังหวะแนวตั้งเดิม (title 0.60→1.40 · rule 1.50 · body 1.85→6.60 ·
footer rule 6.75 · แถวล่าง 6.90), `showMasterSp="0"` และต้องมี `<a:lstStyle>` บนทุก
placeholder ใหม่ — **ข้อหลังนี้พลาดแล้วข้อความจะตกไปใช้ style ของ master ทันที**

เพิ่มใน `scripts/parts_layouts.py` แล้วขยาย `LAYOUTS` ใน `build.py`

| # | Layout | รับสไลด์แบบไหน | แทนที่สไลด์ไหนใน NCT Example |
|---|---|---|---|
| 11 | Split Panel | พาเนลเข้ม + รายการมีเลข + แถบสรุป | S3, S5 |
| 12 | Four Cards + Band | 4 ประเด็นขนาน + แถบสรุปเข้ม | S4 |
| 13 | Process Flow | 3–5 สเต็ปบนแกนเดียว + แถบผลลัพธ์ | S10, S11 |
| 14 | Diagram Canvas | ผังสถาปัตยกรรม / ผังระบบ | S8, S9, S14, S15 |
| 15 | Agenda | คั่นบท + สารบัญ 4–6 บรรทัด | S2, S7, S12 |
| 16 | Dense Table | ตาราง 8–10 แถว | S6, S13 |

### 11 Split Panel

พื้นขาว · `mark-color` มุมขวาบนเหมือน L03

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold `NAVY` |
| Accent Rule | 1.000 | 1.500 | 0.600 | 0.050 | `TEAL` |
| Panel (รูปทรง) | 1.000 | 1.850 | 5.565 | 4.200 | fill `NAVY` |
| Panel kicker PH | 1.300 | 2.100 | 4.965 | 0.400 | 18 pt Kanit Bold `PAPER` |
| Panel body PH | 1.300 | 2.600 | 4.965 | 3.200 | 12 pt dense · `PAPER` alpha 88% |
| Field (รูปทรง) | 6.768 | 1.850 | 5.565 | 4.200 | fill `PAPER2` |
| Field kicker PH | 7.068 | 2.100 | 4.965 | 0.400 | 18 pt Kanit Bold `NAVY` |
| Field body PH | 7.068 | 2.600 | 4.965 | 3.200 | 12 pt dense `INK` |
| Takeaway band | 1.000 | 6.150 | 11.333 | 0.450 | fill `PAPER2` |
| Band label PH | 1.200 | 6.220 | 2.000 | 0.310 | 12 pt Bold `spc 120` `TEAL` |
| Band copy PH | 3.300 | 6.220 | 8.833 | 0.310 | 14 pt `INK` |

- ข้อความบนพาเนลเข้มเป็น `PAPER` เท่านั้น ข้อความรองใช้ alpha ไม่ใช่เทา — กฎ §3 ของ v1
- ฝั่งซ้ายคือ "สภาพปัจจุบัน / บริบท" ฝั่งขวาคือ "สิ่งที่จะเกิดขึ้น" อย่าสลับข้าง
  ทั้งเด็คต้องอ่านทิศทางเดียวกัน

### 12 Four Cards + Band

พื้นขาว · การ์ดเรียงบน `QUARTER`

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold `NAVY` |
| Accent Rule | 1.000 | 1.500 | 0.600 | 0.050 | `TEAL` |
| Card × 4 | 1.000 / 3.883 / 6.766 / 9.649 | 1.850 | 2.683 | 3.800 | fill `PAPER2` **สูงคงที่** |
| Card tab | +0.350 | 2.200 | 0.300 | 0.050 | สีหมวดของการ์ดใบนั้น |
| Card number PH | +0.350 | 2.350 | 1.983 | 0.450 | 24 pt Kanit Bold · สีหมวด |
| Card heading PH | +0.350 | 2.850 | 1.983 | 0.600 | 16 pt Kanit Bold `NAVY` |
| Card body PH | +0.350 | 3.550 | 1.983 | 1.700 | 12 pt dense `INK` |
| ~~Card callout PH~~ | — | — | — | — | ยังไม่ได้ทำใน `parts_layouts.py` — การ์ดมี number / heading / body อย่างละหนึ่ง (idx `i×3+1..3`) |
| Band | 1.000 | 6.000 | 11.333 | 0.600 | fill `NAVY` |
| Band label PH | 1.250 | 6.130 | 2.200 | 0.340 | 12 pt Bold `spc 120` · `PAPER` 70% |
| Band copy PH | 3.550 | 6.120 | 8.583 | 0.360 | 14 pt `PAPER` |

- การ์ดสูงคงที่ 3.80in ข้อความยาวเกินให้ตัดคำ อย่ายืดการ์ด — กฎเดียวกับ L05
- 4 ใบคือเพดาน อยากได้ 5 ประเด็นให้ใช้ L13 หรือแยกสไลด์

### 13 Process Flow

พื้นขาว · สเต็ปเรียงบน `FIFTH` (ใช้ 3–5 ช่อง ถ้าน้อยกว่า 5 ให้จัดกึ่งกลางแถว)

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold `NAVY` |
| Accent Rule | 1.000 | 1.500 | 0.600 | 0.050 | `TEAL` |
| Subtitle PH | 1.000 | 1.850 | 11.333 | 0.450 | 14 pt `INK2` |
| Step × 5 | 1.000 / 3.307 / 5.613 / 7.920 / 10.227 | 2.500 | 2.107 | 2.000 | fill `PAPER2` |
| Step chip | +0.250 | 2.750 | 0.340 | 0.340 | fill `TEAL` · เลข 12 pt `PAPER` |
| Step head PH | +0.250 | 3.200 | 1.607 | 0.450 | 16 pt Kanit Bold `NAVY` |
| Step body PH | +0.250 | 3.700 | 1.607 | 0.700 | 12 pt dense `INK` |
| Connector × 4 | 3.155 / 5.462 / 7.769 / 10.076 | 3.400 | 0.150 | 0.200 | chevron `TEAL` |
| Result band | 1.000 | 4.800 | 11.333 | 0.750 | fill `PAPER2` |
| Result label PH | 1.250 | 4.930 | 2.200 | 0.340 | 12 pt Bold `spc 120` `TEAL` |
| Result copy PH | 3.550 | 4.920 | 8.583 | 0.480 | 14 pt `INK` |
| Control note PH | 1.000 | 5.850 | 11.333 | 0.700 | 12 pt dense `INK2` |

- ลูกศรเป็น chevron ตันสี `TEAL` ห้ามใช้ลูกศรมีหาง มีเงา หรือโค้ง
- สเต็ปคือ **ลำดับเวลา** ถ้าไม่ใช่ลำดับให้ใช้ L12 แทน

### 14 Diagram Canvas

พื้นขาว · layout ที่ตั้งใจให้ว่าง เพราะผังต้องวาดเอง — สิ่งที่ระบบให้คือกรอบกับชุดชิ้นส่วน

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold `NAVY` |
| Accent Rule | 1.000 | 1.500 | 0.600 | 0.050 | `TEAL` |
| Subtitle PH | 1.000 | 1.850 | 11.333 | 0.400 | 14 pt `INK2` |
| *(พื้นที่วาด)* | 1.000 | 2.400 | 11.333 | 3.700 | ไม่มีรูปทรง — วางเองในกรอบนี้ |

> โค้ดฉบับส่งมอบเคยวาดกรอบประไว้ในตำแหน่งนี้ ซึ่งจะติดไปในสไลด์จริงทุกใบ
> ลบออกแล้วตามสเปกข้อนี้ — ถ้าอยากได้เส้นไกด์ ให้เปิด `View > Guides` ใน PowerPoint แทน
| Legend PH | 1.000 | 6.200 | 11.333 | 0.400 | 12 pt dense `INK2` |

**ชุดชิ้นส่วนมาตรฐาน — ทุกผังในเด็คต้องใช้ชุดนี้**

| ชิ้น | สเปก |
|---|---|
| กล่องระบบ | สูง 0.600in · กว้างขั้นต่ำ 1.400in · **มุมตรง** · fill `PAPER2` · เส้น `TEAL` 1 pt · label 12 pt `INK` |
| กล่องหมวด | fill = สีหมวดที่ 15% · เส้น = สีหมวด 1 pt |
| กรอบกลุ่ม | ไม่มี fill · เส้น `RULE` 1 pt ประ · label 12 pt Bold `spc 120` `INK2` มุมซ้ายบน |
| เส้นเชื่อม | `TEAL` 1.25 pt · หัวลูกศรสามเหลี่ยมตัน · **หักมุมฉากเท่านั้น** ห้ามเฉียงหรือโค้ง |
| ป้ายบนเส้น | 10 pt `INK2` พื้น `PAPER` ทับเส้น |

- ห้ามใส่ไอคอน 3-D, เงา, gradient ในผัง — กฎ flat ของ v1 ใช้กับผังด้วย
- ผังที่ต้องใช้เกิน ~30 กล่องแปลว่าควรแยกเป็นสองสไลด์ (งานจริงมีถึง 177 ชิ้นต่อใบ)

### 15 Agenda — ตัวแปรของ 02

โครงเดียวกับ 02 Section Divider ทุกอย่าง (พื้น `NAVY` + แถบภาพขวา, `mark-white`)
เปลี่ยนแค่กล่องล่าง

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Section Photo | 8.000 | 0.000 | 5.333 | 7.500 | เหมือน L02 — ภาพ + fade + scrim ล่าง |
| Section Number PH | 1.000 | 1.600 | 2.500 | 1.100 | 60 pt Kanit Bold `TEAL_L` |
| Accent Rule | 1.000 | 2.800 | 0.600 | 0.050 | `TEAL` |
| Title PH | 1.000 | 2.900 | 6.800 | 0.900 | 40 pt Kanit Bold `PAPER` |
| Agenda list PH | 1.000 | 4.000 | 6.800 | 2.400 | 18 pt · `PAPER` 88% · bullet `TEAL` · **สูงสุด 6 บรรทัด** |

- เลขบทยังต้องพิมพ์เอง ไม่ auto เหมือน L02
- ใช้ L15 ตอนเปิดบท ใช้ L02 ตอนคั่นบทย่อยที่ไม่ต้องลิสต์ — ไม่ใช้สลับกันมั่ว

### 16 Dense Table — ตัวแปรของ 09

| ชิ้น | x | y | w | h | สเปก |
|---|---|---|---|---|---|
| Title PH | 1.000 | 0.600 | 11.333 | 0.800 | 32 pt Kanit Bold `NAVY` |
| Accent Rule | 1.000 | 1.500 | 0.600 | 0.050 | `TEAL` |
| Intro PH | 1.000 | 1.850 | 11.333 | 0.400 | 12 pt dense `INK2` |
| Table PH | 1.000 | 2.350 | 11.333 | 3.850 | รับ 8–10 แถวที่ 10 pt |
| Footnote PH | 1.000 | 6.300 | 11.333 | 0.300 | 10 pt `INK2` |

**Table style**

| ส่วน | สเปก |
|---|---|
| แถวหัว | fill `NAVY` · สูง 0.380in · 11 pt Bold `spc 60` `PAPER` |
| แถวเนื้อ | สลับ `PAPER` / `PAPER2` · สูง 0.385in · 10 pt `INK` |
| เส้นตาราง | `RULE` 0.5 pt แนวนอนเท่านั้น — **ไม่มีเส้นแนวตั้ง** ใช้ช่องไฟแทน |
| คอลัมน์แรก | chip 0.280in สี่เหลี่ยมจัตุรัส สีหมวด · เลข 10 pt `PAPER` |
| Status cell | พื้น tint (`RISK_T` / `WARN_T` / `OK_T`) · ตัวอักษรสีเข้มคู่กัน 10 pt |
| padding cell | 0.080in ทุกด้าน |

- 10 แถวคือเพดาน เกินกว่านั้นแยกสไลด์ อย่าลดขนาดตัวอักษร
- คอลัมน์ที่มีข้อความยาว ๆ หลายบรรทัดไม่ควรอยู่ในตาราง — ย้ายไปเป็น L11 หรือ L13

---

## 6. กติกาเพิ่มเติมสำหรับ proposal deck

เสริมจาก §6 ของ v1 ไม่ได้แทนที่

**ทำ**
- เด็คข้อเสนอเดินตามลำดับ: ปก → agenda → บริบท → ปัญหา → วัตถุประสงค์ → ขอบเขต →
  แนวทางรายกระบวนการ → แผน → ปิด
- สไลด์ตารางและสไลด์ผังต้องมีแถบสรุป 1 บรรทัดเสมอ (`Takeaway` / `Result` band) —
  ผู้อ่านต้องได้ข้อสรุปโดยไม่ต้องอ่านทั้งตาราง
- ผูกสีหมวดครั้งเดียวตอนเริ่มเด็ค เขียนไว้ในสไลด์ agenda ด้วย
- ภาคผนวกวางหลังหน้าปิด และเริ่มด้วย L15 ที่เขียนว่า "ภาคผนวก" ให้ชัด

**ห้าม**
- ย่อตัวอักษรต่ำกว่า 10 pt ไม่ว่ากรณีใด
- เอา dense role ไปใช้กับ L03 / L04 / L05
- paste สไลด์จากไฟล์อื่นแบบ keep formatting — copy เฉพาะข้อความลง placeholder เท่านั้น
  (นี่คือสาเหตุของสไลด์ 16–25 ใน `NCT Example.pptx` ที่ล้นขอบและใช้ฟอนต์แบรนด์อื่น)
- ใช้ `TEAL_B` นอกจาก gradient stop
- ใช้สี status ทำหัวเรื่อง เส้น หรือพื้นสไลด์

---

## 6.1 สิ่งที่พบตอนประกอบจริง (build + render ตรวจแล้ว)

ทั้งหมดแก้แล้วในโค้ด บันทึกไว้กันพลาดซ้ำ

| จุด | อาการ | แก้เป็น |
|---|---|---|
| `build.py` | `LAYOUTS` ยังมีแค่ 10 ขณะที่ `parts_master.py` ประกาศ 16 id → master อ้าง `rId11–16` ที่ไม่มีใน rels | ลงทะเบียน 11–16 และให้ master รับ `n_layouts` จาก `len(LAYOUTS)` ไม่ hardcode |
| `tableStyles.xml` | PowerPoint ปฏิเสธไฟล์ทั้งใบ (`0x80070570`) | `<a:font>` ใน `a:tcTxStyle` เป็น `CT_FontCollection` ต้องมี `latin` + `ea` + `cs` ครบสาม ขาดตัวใดตัวหนึ่งไฟล์เปิดไม่ได้ |
| `<a:ln>` | กรอบประในผังทำไฟล์พัง | ลำดับลูกของ `a:ln` ตายตัว — fill มาก่อน `prstDash` เสมอ |
| L11 panel body | ใช้ `body_specs()` (สี `INK`) บนพาเนล `NAVY` → น้ำเงินบนน้ำเงิน มองไม่เห็น | เพิ่ม `dense_specs()` และใช้ `PAPER` alpha 88% ตามสเปก §11 |
| L15 agenda list | ไม่มี hanging indent bullet ชนข้อความ | ตั้ง `marL` / `indent` 0.30in |

> บทเรียนเดิมย้ำอีกครั้ง: XML well-formed ไม่ได้แปลว่า schema ถูก ต้องเปิดด้วย PowerPoint จริงทุกครั้ง

---

## 7. สรุปการเปลี่ยนแปลง

| หมวด | v1 | v2 |
|---|---|---|
| Color token | 9 | 9 + `MID` + `TEAL_B` + status 6 = **17** |
| Type role | 13 | 13 + dense 7 = **20** |
| Layout | 10 | **16** |
| Grid unit | HALF, THIRD | + QUARTER, FIFTH |
| พื้นล่างขนาดตัวอักษร | 10 pt (โดยปริยาย) | **10 pt (บังคับ)** |
| ฟอนต์ | Kanit + Noto Sans Thai | ไม่เปลี่ยน |

**ยังไม่มีใน v2** — chart style กำหนดเอง, `Card callout PH` ของ L12, layout timeline / team grid / org chart / pricing
และโลโก้ขาวยังเป็น silhouette จากการ knockout JPEG (ถ้ามีไฟล์ SVG ต้นฉบับ ให้แทนที่
`assets/nct-logo-white.png` แล้ว rebuild)
